{% doc %}
  @prompt
    EMPTY SECTION
{% enddoc %}
{% assign ai_gen_id = block.id | replace: '_', '' | downcase %}

{% style %}
  .ai-empty-section-{{ ai_gen_id }} {
    display: block;
    width: {{ block.settings.desktop_width_percent }}%;
    max-width: 100%;
    height: {{ block.settings.height }}px;
    background: transparent;
  }

  @media screen and (max-width: 749px) {
    .ai-empty-section-{{ ai_gen_id }} {
      height: {{ block.settings.mobile_height }}px;
    }
  }
{% endstyle %}

<div
  class="ai-empty-section-{{ ai_gen_id }}"
  aria-hidden="true"
  {{ block.shopify_attributes }}
></div>

{% schema %}
{
  "name": "Empty section",
  "settings": [
    {
      "type": "range",
      "id": "desktop_width_percent",
      "label": "Desktop width",
      "min": 50,
      "max": 100,
      "step": 5,
      "unit": "%",
      "default": 100
    },
    {
      "type": "range",
      "id": "height",
      "label": "Desktop height",
      "min": 0,
      "max": 300,
      "step": 10,
      "unit": "px",
      "default": 40
    },
    {
      "type": "range",
      "id": "mobile_height",
      "label": "Mobile height",
      "min": 0,
      "max": 200,
      "step": 10,
      "unit": "px",
      "default": 20
    }
  ],
  "presets": [
    {
      "name": "Empty section"
    }
  ],
  "tag": null,
  "class": "empty-section-block"
}
{% endschema %}